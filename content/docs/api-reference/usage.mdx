---
title: Usage & Billing
description: Monitor API usage and understand billing tiers.
---

## Get Usage Summary

```http
GET /v1/usage
```

Optional query: `?period=2025-03` (format: YYYY-MM, defaults to current month).

**Response** `200`:

```json
{
  "period": "2025-03",
  "api_calls": 4521,
  "repos_created": 3,
  "git_transfer_bytes": 1048576,
  "storage_bytes": 5242880
}
```

## Get Usage Details

```http
GET /v1/usage/details?limit=100&offset=0
```

- `limit` — Max results (default: 100, max: 500)
- `offset` — Skip items (default: 0)
- `cursor` — Keyset pagination cursor

**Response** `200`:

```json
{
  "events": [
    {
      "id": 1,
      "event_type": "api_call",
      "quantity": 1,
      "metadata": null,
      "recorded_at": "2025-03-15T12:00:00Z"
    }
  ],
  "limit": 100,
  "offset": 0,
  "next_cursor": null
}
```

## Tiers

| | Free | Usage |
|---|---|---|
| Repos | 5 | Unlimited |
| API calls | 10,000 / month | Unlimited ($0.50 / 1K calls) |
| Git transfer | 5 GB / month | Unlimited ($0.10 / GB) |
| Storage | 1 GB | Unlimited ($0.10 / GB / month) |
| Price | $0 | Pay-as-you-go |

Upgrade to the Usage tier from your [Dashboard](https://app.coregit.dev/dashboard/billing).
